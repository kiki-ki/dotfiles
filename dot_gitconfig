[user]
  email = malpie0619@gmail.com
  name = kiki-ki

[core]
  editor = vi
  ignorecase = false
  pager = "sh -c 'if command -v bat >/dev/null 2>&1; then bat --paging=always; else less; fi'"

[pull]
  rebase = false

[merge]
  conflictstyle = diff3

[include]
  path = ~/.gitconfig_shared

[alias]
  pushf = push --force-with-lease --force-if-includes
